.chat-group-form
  %h1 新規チャットグループ
  = form_for(@chat_group, html: { id: "new_chat_group" }) do |f|
    .chat-group-form__field
      .chat-group-form__field--left
        = f.label :name, "グループ名", class: "chat-group-form__label"
      .chat-group-form__field--right
        = f.text_field :name, class: "chat-group-form__input", placeholder: "グループ名を入力してください"
    .chat-group-form__field
      .chat-group-form__field--left
        .chat-group-form__label チャットメンバーを追加
      #result.chat-group-form__field--right
        = f.text_field :group_users, value: "", placeholder: "追加したいユーザー名を入力してください", class: "chat-group-form__input"
        - @users.each do |user|
          #user_info.chat-group-user
            %p.chat-group-user__name
              = user.name
            .chat-group-user__btn
              %p#btn.chat-group-user__btn--add{ data: { user: { id: user.id } } } 追加
    .chat-group-form__field
      .chat-group-form__field--left
        .chat-group-form__label チャットメンバー
      #added_users.chat-group-form__field--right
        .chat-group-user
          .chat-group-user__name
            = current_user.name
    .chat-group-form__field
      .chat-group-form__field--left
      .chat-group-form__field--right
        = f.submit "Save", class: "chat-group-form__action-btn"
